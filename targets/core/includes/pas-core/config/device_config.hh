#ifndef PAS_CONFIG_DEVICE_CONFIGURATION_HH
#define PAS_CONFIG_DEVICE_CONFIGURATION_HH

#include <string>

namespace pas::config {

struct DeviceConfiguration {
  std::string icon;
};

}  // namespace pas::config

#endif
