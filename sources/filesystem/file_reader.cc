#include "file_reader.hh"

#include <filesystem>

namespace pas::filesystem {

auto FileReader::Read(const std::filesystem::path& file_path) -> std::string { return ""; }
}  // namespace pas::filesystem
